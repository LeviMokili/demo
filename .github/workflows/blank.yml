name: Hello World
on: workflow_dispatch
jobs:
 hello:
   runs-on: ubuntu-latest
   steps:
     - name: Hello step
       run: |
        echo "welcome to github actions One"
        echo "welcome to github actions Two"
        echo "welcome to github actions Three"
